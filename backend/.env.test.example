# Dedicated MySQL database used by migration and integration checks.
# Copy to .env.test and replace the credentials for your local environment.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=durisweb_test
DB_PASSWORD=replace-me
DB_NAME=durisweb_test
